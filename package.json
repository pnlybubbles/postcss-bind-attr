{
  "name": "postcss-bind-attr",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "standard && dependency-check . && tape test/index.js",
    "test:debug": "node --inspect --debug-brk test/index.js",
    "fmt": "standard --fix"
  },
  "dependencies": {
    "postcss": "^5.2.16",
    "postcss-selector-parser": "^2.2.3"
  },
  "devDependencies": {
    "dependency-check": "^2.8.0",
    "standard": "^9.0.2",
    "tape": "^4.6.3"
  }
}
